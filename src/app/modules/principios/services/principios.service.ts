import { Injectable } from '@angular/core';

/**
 * Centraliza el acceso a los datos y operaciones del módulo de principios.
 *
 * Está disponible en toda la aplicación mediante el inyector raíz. Por ahora
 * no expone métodos porque la fuente de datos del módulo aún no está definida.
 */
@Injectable({
  providedIn: 'root'
})
export class PrincipiosService {
}
